import { Theme } from '@leafygreen-ui/lib';
import { ChartOptions } from '../Chart.types';
/**
 * Returns default chart options that are shared by all charts.
 */
export declare const getDefaultChartOptions: (theme: Theme) => Partial<ChartOptions>;
//# sourceMappingURL=getDefaultChartOptions.d.ts.map
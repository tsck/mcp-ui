{
  "name": "mcp-poc",
  "version": "1.0.0",
  "description": "UI augmentation for Model Context Protocol (MCP) servers",
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "build:bundles": "pnpm --filter @mcp-poc/mcp-ui-sdk build",
    "start": "pnpm --filter @mcp-poc/mcp-server start",
    "server:dev": "pnpm --filter @mcp-poc/mcp-server dev",
    "client:dev": "pnpm --filter @mcp-poc/mcp-client dev",
    "dev": "pnpm build:bundles && pnpm concurrently \"pnpm --filter @mcp-poc/mcp-server dev\" \"pnpm client:dev\" --names \"SERVER,CLIENT\" --prefix-colors \"blue,green\""
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@mcp-ui/server": "^5.12.0"
  }
}

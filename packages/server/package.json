{
  "name": "@mcp-ui/server",
  "version": "1.0.0",
  "description": "MCP UI Server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "pnpm build:twig && tsx src/index.ts",
    "dev": "pnpm build:twig && tsx watch src/index.ts",
    "build:twig": "pnpm --filter @mcp-ui/twig build"
  },
  "dependencies": {
    "@mcp-ui/twig": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.20.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "tsx": "^4.20.6"
  }
}
